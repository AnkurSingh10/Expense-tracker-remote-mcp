[project]
name = "remote-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AnkurSingh10", email = "ankurrssingh110@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
	"aiosqlite>=0.22.1",
	"fastmcp>=4.0.5",
	"psycopg[binary]>=3.2",
	"python-dotenv>=1.2.3",
]

[project.scripts]
remote-server = "remote_server:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
